{
  "name": "i4-currency-converter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:api": "cd api && python3 -m pytest -v",
    "test:cli": "cd client && npm test",
    "test:all": "npm run test:api && npm run test:cli",
    "verify": "./scripts/verify.sh"
  }
}
