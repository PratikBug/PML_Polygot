# B5 — Node.js Transaction API

Node.js Express API mirroring the B4 FastAPI transaction service.

## Endpoints

Same as B4: `POST /transactions`, `GET /transactions`, `GET /balance`, `GET /health`

## Install & Run

```bash
npm install
npm start
```

## Test

```bash
npm test
```
